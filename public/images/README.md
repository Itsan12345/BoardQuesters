# Project Images

Place all your static image assets (PNG, JPG, SVG, etc.) in this directory.

In your Next.js components, you can reference these images using the absolute path:
`<Image src="/images/your-image-name.png" ... />`

Remember to update `src/app/lib/placeholder-images.json` if you want to use these as managed placeholders.